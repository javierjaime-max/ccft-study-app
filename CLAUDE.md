# CLAUDE.md — ccft-study-app

## Ecosystem: CrossFit OTL
This repo is part of the **CrossFit OTL ecosystem** — NOT Lifestyle OS. Different brand, different voice, different audience.

Connected components: CrossFit OTL gym (`Fit4/CrossFit OTL/`), CCFT-Brain knowledge base (`Fit4/CrossFit OTL/CCFT-Brain/`).

## What This Is
A study app for the CrossFit Certificate (CCFT) exam. React + Vite + TypeScript. Backend is Supabase.

## Deploy Pipeline — ONE PATH ONLY
```
Edit files locally → git commit → git push → Vercel auto-deploys
```
Never use `vercel deploy` CLI. Never edit files in the GitHub web editor.

## Session Start — Do This First
```bash
git pull
```

## Session End — Do This Before Closing
```bash
git push origin main
```

## Tech Stack
- **Frontend:** React, Vite, TypeScript
- **Backend:** Supabase
- **Deployment:** Vercel

## Git Config
```bash
git config user.email "javier.jaime@me.com"
```
